#+title: lucasvreis

Essa é a minha página pessoal. Bem vindos! 🙂

Algumas coisas para se fazer aqui:
- [[http:blog][Postagens do blog]]

Alguns links de fora:
- [[https://github.com/lucasvreis][Meu Github]]

Feito com Haskell!

#+begin_export html
<script src="//cdn.jsdelivr.net/npm/element-resize-detector@1.2.4/dist/element-resize-detector.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/d3-force-3d@3.0.2/dist/d3-force-3d.min.js"></script>
<div id="graph"
     style="width: 400px;
         max-width: 90%;
         border-radius: 100%;
         border-style: dashed;
         border-width: 1px;
         margin: auto;
         height: 400px;
         overflow: hidden;">
</div>
<script>
 function renderGraph () {
   var url = '/zettelkasten/graph.json';
   var parser = new DOMParser();
   var serializer = new XMLSerializer();
   var first = true
   var Graph = ForceGraph()(document.getElementById('graph'))
     .zoom(1.6)
     .cooldownTicks(100)
   // .enableZoomInteraction(false)
     .nodeLabel(node => {
       var label = parser.parseFromString("<span style=\"font-size: 11pt; font-family: sans \">" + node.name + "</span>", "text/xml");
       renderMathInElement(label, katexOptions);
       return serializer.serializeToString(label);
     });

   fetch(url).then(res => res.json()).then(out => {
     Graph.graphData(out)
          .linkDirectionalParticles(1)
          .linkCurvature('curvature')
          .nodeRelSize(4)
          .nodeAutoColorBy('id')
          .onNodeClick((node, e) => window.location = '/zettelkasten/' + node.id);

     Graph.d3Force('x', d3.forceX().strength(0.2));
     Graph.d3Force('y', d3.forceY().strength(0.2));
     Graph.d3Force('charge').strength(-60);
     Graph.d3Force('link').strength(0.2);
     Graph.onEngineStop(() => {
       if (first) {
         Graph.zoomToFit(100, 30);
         first = false;
       }
     });
   });
   elementResizeDetectorMaker().listenTo(
     document.getElementById('graph'),
     function(element) {
       Graph.height(element.offsetHeight)
       Graph.width(element.offsetWidth)
     }
   );
}
</script>
<script src="//unpkg.com/force-graph"
        onload="renderGraph();"></script>
#+end_export

